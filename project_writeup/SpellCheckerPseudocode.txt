Spell Checker Menu Pseudocode

	//check if the user has generated a word file already
	if file doesn't exist then
		return error

	print available word files that can be used
	let Action := user inputted character

	If Action = letter describing a word file then
		MethodCheck(Action)
	else if Action = Z then
		return to main menu
	else
		loop till valid user input is given

function MethodCheck() is
	input: character Action
	ouput: integer returnCode

	//(A) list with linear search
	//(B) list with binary search
	//(C) Hash Table
	//(Z) return to previous menu
	print out options for user

	let secondAction := user inputted character

	if secondAction = A or B or C then
		SpellCheck(Action, secondAction)

	else if secondAction = Z then
		return back to spell checker menu
	else
		loop till valid user input is given

function SpellCheck() is
	input: character Action, character secondAction
	output: integer returnCode

	//(A) no suggestions
	//(B) levenshtein Distance
	//(C) Hamming Distance
	//(D) Sorensen-Dice Coefficient
	//(E) Optimal String Alignment Distance
	//(F) Damerau-Levenshtein Distance
	//(G) Jaro Similarity
	//(H) Jaro-Winkler Similarity
	print out he options for user

	let thirdAction := user inputted character

	if thirdAction = A through to H then
		SpellChecker(Action, secondAction, thirdAction)

	else if thirdAction = Z then
		return back to MethodCheck() function

	else
		loop till valid user input is given

function SpellChecker() is
	input: character Action, character secondAction, character thirdAction
	output: integer returnCode
	